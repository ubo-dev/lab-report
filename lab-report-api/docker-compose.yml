version: '3.9'

services:
  db:
    image: mysql:8.0
    command: --authentication_policy=caching_sha2_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: passwd1
      MYSQL_DATABASE: labreport-mysql
      MYSQL_USER: test-user
      MYSQL_PASSWORD: userpasswd1
    ports:
      - "3306:3306"

